export enum EmotionalReadiness {
  Ready = 'ready',
  AlmostReady = 'almost_ready',
  NeedMoreTime = 'need_more_time',
}

export enum CommunicationStyle {
  Direct = 'direct',
  Gentle = 'gentle',
  Reflective = 'reflective',
  Expressive = 'expressive',
}

export enum ConflictResolutionStyle {
  TalkItOut = 'talk_it_out',
  NeedSpace = 'need_space',
  SeekMediation = 'seek_mediation',
  Avoid = 'avoid',
}
