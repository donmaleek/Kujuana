describe('Matching', () => {
  it.todo('standard matching produces up to 5 scored matches');
  it.todo('priority matching deducts 1 credit');
  it.todo('priority matching fails with insufficient credits');
  it.todo('standard-tier user cannot request priority match');
  it.todo('VIP curation only includes profiles with ≥90% completeness');
});
