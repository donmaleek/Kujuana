# ══════════════════════════════════════════════════════════════════════
# Kujuana Web App — Production Environment Variables
# Copy to .env.production.local and fill in values.
# ══════════════════════════════════════════════════════════════════════

# ── API ──────────────────────────────────────────────────────────────
# URL of your production API (no trailing slash)
NEXT_PUBLIC_API_URL=https://api.kujuana.com/api/v1

# Internal server-side API URL (used during SSR/ISR, optional)
API_URL=http://localhost:4000/api/v1

# ── Mobile App Download ───────────────────────────────────────────────
# URL where the Android APK is hosted on your VPS.
# After building with: eas build --platform android --profile apk
# Download the .apk from EAS and upload it to your server, then set this.
# Served by nginx at: /var/www/kujuana/downloads/kujuana.apk
NEXT_PUBLIC_APK_URL=https://kujuana.com/downloads/kujuana.apk

# ── App metadata ─────────────────────────────────────────────────────
NEXT_PUBLIC_APP_NAME=Kujuana
NEXT_PUBLIC_APP_URL=https://kujuana.com

# ══════════════════════════════════════════════════════════════════════
# APK HOSTING SETUP (on your VPS)
# ══════════════════════════════════════════════════════════════════════
#
# 1. BUILD THE APK
#    cd apps/mobile
#    npx eas build --platform android --profile apk
#    # Download the .apk file from the EAS dashboard or the CLI link
#
# 2. UPLOAD TO YOUR VPS
#    mkdir -p /var/www/kujuana/downloads
#    scp kujuana.apk user@your-server:/var/www/kujuana/downloads/kujuana.apk
#
# 3. SERVE WITH NGINX
#    Add to your nginx site config:
#
#    location /downloads/ {
#        alias /var/www/kujuana/downloads/;
#        add_header Content-Disposition 'attachment; filename="kujuana.apk"';
#        add_header Content-Type application/vnd.android.package-archive;
#        add_header X-Content-Type-Options nosniff;
#    }
#
# 4. TEST THE DOWNLOAD
#    curl -I https://kujuana.com/downloads/kujuana.apk
#    # Should return 200 with Content-Type: application/vnd.android.package-archive
#
# 5. UPDATE APK VERSION
#    For new releases: rebuild with eas, re-upload the file (same filename).
#    The /download page QR code updates automatically since it points to the URL.
